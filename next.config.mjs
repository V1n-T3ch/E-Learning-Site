/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [
            'randomuser.me',
            'imgs.search.brave.com'
        ],
    },
};

export default nextConfig;
